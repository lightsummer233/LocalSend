name: cli
description: A sample command-line application.
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.1.1

dependencies:
  args: ^2.7.0
  common:
    path: ../common
  path: ^1.9.1

dev_dependencies:
  lints: ^6.1.0
  test: ^1.21.0
